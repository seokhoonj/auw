% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/clean.R
\name{add_age_band}
\alias{add_age_band}
\title{Create age bands}
\usage{
add_age_band(
  df,
  age_var,
  interval = 5,
  right = FALSE,
  col_nm = "age_band",
  cutoff = FALSE,
  label_style = c("close", "open.start", "open", "open.end")
)
}
\arguments{
\item{df}{A data.frame containing an age column.}

\item{age_var}{Unquoted column name specifying the age variable.}

\item{interval}{Numeric; the width of each age interval. Default: \code{5}.}

\item{right}{Logical; whether intervals should be closed on the right
(and open on the left) or vice versa. Default: \code{FALSE}.}

\item{col_nm}{String; name of the new age band column. Default \code{"age_band"}.}

\item{cutoff}{Logical; if \code{TRUE}, ensures the maximum observed age
is included as an upper cutoff. Default: \code{FALSE}.}

\item{label_style}{One of \code{"close"}, \code{"open"}, \code{"open.start"}, \code{"open.end"};
controls how labels are displayed at boundaries:
\itemize{
\item \code{"close"}: closed intervals like \code{"0-4"}, \code{"5-9"}.
\item \code{"open"}: first/last open (e.g. "\code{-4}", "\verb{95-}").
\item \code{"open.start"}: only first open (e.g. "\code{-4}", "\verb{5-9"}).
\item \code{"open.end"}: only last open (e.g. "\verb{95-}").
}}
}
\value{
The input \code{df}, augmented with a new ordered factor column
(by default \code{"age_band"}).
}
\description{
A convenience wrapper around \code{\link[=add_band]{add_band()}} to generate categorical
\strong{age bands} from a numeric age column. The function creates an
ordered factor column representing grouped age intervals.
}
\examples{
\donttest{
df <- data.frame(age = sample(0:99, 20, replace = TRUE))

# Default 5-year bands
add_age_band(df, age)

# Custom 10-year bands with open-ended labels
add_age_band(df, age, interval = 10, label_style = "open.end")
}

}
