% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/kcd.R
\name{add_kcd_name}
\alias{add_kcd_name}
\title{Add human-readable KCD names (ko/en)}
\usage{
add_kcd_name(df, kcd_var, dots = TRUE, lang = c("ko", "en"))
}
\arguments{
\item{df}{A data frame / tibble / data.table.}

\item{kcd_var}{Column with KCD codes; \strong{unquoted} name (e.g., \code{kcd})
or \strong{character scalar} (e.g., \code{"kcd"}).}

\item{dots}{Logical. If \code{TRUE} (default), treat dot-variants in codes as
equivalent (e.g., \code{"M51"} matches \code{"M51.0"}); implemented by removing
punctuation in the copybook before join.}

\item{lang}{Language of the name column to add; one of \code{"ko"} or \code{"en"}.}
}
\value{
An object of the \strong{same class as \code{df}}, with an added column named
\code{paste0(kcd_var, "_", lang)} containing the mapped term.
}
\description{
Join a codebook to append a human-readable KCD name column (\verb{*_ko} or \verb{*_en})
next to the KCD code column.
}
\details{
A (right) join is performed against the internal KCD copybook. The copybook
column is temporarily renamed to match \code{kcd_var} so that a named-equality
join can be performed; the result is restored to the original class of \code{df}.
}
\examples{
\dontrun{
df2 <- add_kcd_name(df, kcd, lang = "ko")  # unquoted
df3 <- add_kcd_name(df, "kcd", dots = TRUE, lang = "en")
}

}
\seealso{
\code{\link[=add_kcd_sub]{add_kcd_sub()}}, \code{\link[=get_kcd]{get_kcd()}}
}
