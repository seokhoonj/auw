% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/kcd.R
\name{add_kcd_sub}
\alias{add_kcd_sub}
\title{Add KCD subcode columns}
\usage{
add_kcd_sub(df, kcd_var, digit = c(3L, 2L, 1L))
}
\arguments{
\item{df}{A data frame / tibble / data.table.}

\item{kcd_var}{Column containing KCD codes; \strong{unquoted} name (e.g., \code{kcd})
or \strong{character scalar} (e.g., \code{"kcd"}).}

\item{digit}{Integer vector of substring widths to extract (default \code{c(3,2,1)}).}
}
\value{
An object of the \strong{same class as \code{df}}, with new columns named
\code{paste0(kcd_var, digit)} inserted after \code{kcd_var}.
}
\description{
Create substring columns from a KCD code column (e.g., first 3/2/1 chars).
Useful for grouping by broader sections of the code.
}
\examples{
\dontrun{
df2 <- add_kcd_sub(df, kcd)         # unquoted
df3 <- add_kcd_sub(df, "kcd", 3:1)  # character + custom widths
}

}
\seealso{
\code{\link[=add_kcd_name]{add_kcd_name()}}, \code{\link[=get_kcd]{get_kcd()}}
}
