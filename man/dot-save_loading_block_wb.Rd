% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/relative-risk-save.R
\name{.save_loading_block_wb}
\alias{.save_loading_block_wb}
\title{Save a loading-table block to a workbook (internal)}
\usage{
.save_loading_block_wb(
  wb,
  sheet,
  suffix_tables,
  rc = c(3L, 7L),
  row_spacer = 2L,
  section_title = NULL,
  section_title_size = 14,
  data_titles = NULL,
  data_title_size = 11,
  font_size = 11,
  auto_width = FALSE,
  num_fmt = "0.00",
  row_names = FALSE
)
}
\arguments{
\item{wb}{An \code{openxlsx} \code{Workbook}.}

\item{sheet}{Sheet name.}

\item{suffix_tables}{A list of loading tables (typically suffix tables)
written vertically to the worksheet.}

\item{rc}{Start cell (\code{c(row, col)}) for the block. If \code{section_title} is set,
the title is written at \code{rc} and tables start one row below.}

\item{row_spacer}{Blank rows between stacked tables.}

\item{section_title}{Optional section heading written above the block.}

\item{section_title_size}{Font size for \code{section_title}.}

\item{data_titles}{Optional character vector of per-table titles passed to
\code{save_data_wb()}.}

\item{data_title_size}{Font size for per-table titles in \code{save_data_wb()}.}

\item{font_size}{Font size for table bodies.}

\item{auto_width}{Logical; passed to \code{save_data_wb()}.}

\item{num_fmt}{Numeric format passed to \code{save_data_wb()}.}

\item{row_names}{Logical; passed to \code{save_data_wb()}.}
}
\value{
The updated workbook.
}
\description{
Writes a block consisting of:
\itemize{
\item An optional section title (e.g., "Relative Risk", "Mean Ratio - HOS")
\item One or more loading tables stacked vertically (via \code{save_data_wb()})
\item A bold, underlined total under the \code{wt_loading} column for each table
}
}
\details{
Requires \code{save_data_wb()} to record table ranges in \code{attr(wb, "instead.ranges")}.
}
\keyword{internal}
