% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/relative-risk.R
\name{.summarise_mr_core}
\alias{.summarise_mr_core}
\title{Get mean ratios (core engine)}
\usage{
.summarise_mr_core(
  x,
  outcome = c("hos", "sur"),
  decl_vs = c("0", "1"),
  conf.level = 0.95,
  correction = TRUE
)
}
\arguments{
\item{x}{An \code{id_data} object containing mean-outcome summaries in
\code{attr(x, "summary.hos")} and/or \code{attr(x, "summary.sur")}.}

\item{outcome}{Character scalar. The outcome column to summarise;
one of \code{"hos"} or \code{"sur"}.}

\item{decl_vs}{Character vector of length 2 giving the declaration levels
to compare (e.g., \code{c("0", "1")}).}

\item{conf.level}{Confidence level for intervals (default \code{0.95}).}

\item{correction}{Logical. If \code{TRUE}, applies a +0.5 continuity correction
to zero totals (\code{v0} or \code{v1}) for stability of log/SE calculations.}
}
\value{
A restored data frame with prepended class \code{"mr"} containing
per-stratum results. Columns include:
\itemize{
\item \code{n0}, \code{n1}: ID counts in reference vs. exposed groups,
\item \code{v0}, \code{v1}: outcome totals in reference vs. exposed groups,
\item \code{rate0}, \code{rate1}: per-ID means in each group,
\item \code{mr}, \code{mr_lower}, \code{mr_upper}: MR and its Wald confidence interval,
\item \code{p_value}: two-sided Wald test p-value for \code{H0: MR = 1},
\item \code{decision}: factor \code{"reject"} / \code{"fail"} at level \code{conf.level}.
}

Metadata attributes (\code{"decl"}, \code{"excl"}, \code{"claim"}) are propagated
from the underlying \code{"mo"} summary when present.
}
\description{
Compute mean ratios (MR) and their confidence intervals from an
\code{id_data} object for count outcomes such as total hospital days
(\code{hos}) or surgeries (\code{sur}).
}
\details{
This is the internal engine used by \code{summarise_mr.id_data()}.
The function internally retrieves the mean-outcome summary table
stored at \code{attr(x, paste0("summary.", outcome))}
(class \code{"mo"}).

The MR here is a \strong{ratio of per-ID means}:

\code{MR = (v1 / n1) / (v0 / n0)},

where:
\itemize{
\item \code{v0}, \code{v1} are the summed outcome totals in the two declaration groups,
\item \code{n0}, \code{n1} are the corresponding ID counts.
}

Confidence intervals are computed on the log scale using a Wald
approximation:

\verb{se(log(MR)) ≈ sqrt(1/v1 + 1/v0)},

optionally with a +0.5 continuity correction when \code{v0} or \code{v1} are zero.
}
\keyword{internal}
