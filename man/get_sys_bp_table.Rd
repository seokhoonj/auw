% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/bp.R
\name{get_sys_bp_table}
\alias{get_sys_bp_table}
\title{Retrieve systolic blood pressure distribution table from KOSIS}
\usage{
get_sys_bp_table(age_interval = 5, period = "latest")
}
\arguments{
\item{age_interval}{Integer; width of the age band (default: 5).
Passed to \code{\link[=add_age_band]{add_age_band()}} to group ages.}

\item{period}{Either \code{"latest"} (default, the most recent year available) or
a specific survey year (numeric string, e.g. \code{"2020"}).}
}
\value{
A data.table with columns:
\describe{
\item{bp_type}{Blood pressure type, \code{"systolic"}}
\item{age_band}{Age band factor (from \code{add_age_band})}
\item{gender}{Factor: M = Male, F = Female}
\item{bp}{Ordered factor of blood pressure categories (e.g. \code{"<120"}, \code{">=140"})}
\item{year}{Survey year (numeric)}
\item{n}{Estimated number of respondents in the cell}
}
}
\description{
Downloads and processes the \strong{systolic blood pressure} table from the
Korean Statistical Information Service (KOSIS).
Data are grouped by age, gender, blood pressure category, and survey year.
}
\examples{
\dontrun{
# Latest available systolic blood pressure distribution
sys <- get_sys_bp_table()
head(sys)

# Specific year
sys20 <- get_sys_bp_table(period = "2023")
}

}
