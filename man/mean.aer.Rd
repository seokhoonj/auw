% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/aer.R
\name{mean.aer}
\alias{mean.aer}
\title{Compute group-wise mean A/E Ratio / Cumulative A/E Ratio}
\usage{
\method{mean}{aer}(x, ...)
}
\arguments{
\item{x}{An object of class \code{aer}.}

\item{...}{Unused; included for S3 compatibility.}
}
\value{
A \code{data.table} grouped grid with the columns above. The result keeps
the attributes \code{group_var} and \code{elapsed_var}, and its class is updated to
include \code{aer_mean} (via \code{instead::update_class()}).
}
\description{
S3 method for \code{mean()} on \code{aer} objects. Aggregates by the variables stored
in attributes \code{group_var} and \code{elapsed_var}, computing the mean and standard
error (SE) of both \code{aer} and \code{caer}. SE bands are reported as mean ± mean/√n.
}
\details{
The function expects the input to be a \code{data.table}-like object with columns
\code{aer} and \code{caer}. Grouping variables are retrieved from
\code{attr(x, "group_var")}, and the elapsed dimension from
\code{attr(x, "elapsed_var")}. Output columns include:
\itemize{
\item \code{n_sample}: number of observations in the cell
\item \code{aer_mean}, \code{caer_mean}: group-wise means
\item \code{aer_se}, \code{caer_se}: standard errors \code{mean / sqrt(n)}
\item \code{aer_se_lwr}, \code{aer_se_upp}, \code{caer_se_lwr}, \code{caer_se_upp}: SE bands
}
}
\examples{
\donttest{
mt <- mean(aer_obj)
head(mt)
}

}
