% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/population.R
\name{plot.population}
\alias{plot.population}
\title{Plot method for population objects}
\usage{
\method{plot}{population}(
  x,
  show_label = TRUE,
  label_args = list(family = getOption("ggshort.font"), size = 4, angle = 90, hjust =
    -0.1, vjust = 0.5, color = "black"),
  theme = c("view", "save", "shiny"),
  ...
)
}
\arguments{
\item{x}{A \code{population} object returned by \code{\link[=summarise_population]{summarise_population()}}.}

\item{show_label}{Logical; if \code{TRUE} (default), display count labels
above each bar.}

\item{label_args}{A named list of arguments controlling label appearance,
passed to \code{\link[ggshort:ggbar]{ggshort::ggbar()}}. Common options include:
\code{family}, \code{size}, \code{angle}, \code{hjust}, \code{vjust}, \code{color}.}

\item{theme}{One of \code{"view"}, \code{"save"}, or \code{"shiny"}. Controls the
ggplot2 theme applied via \code{\link[ggshort:switch_theme]{ggshort::switch_theme()}}.}

\item{...}{Additional arguments forwarded to \code{\link[=plot_population]{plot_population()}}.}
}
\value{
A ggplot object created by \code{\link[=plot_population]{plot_population()}}.
}
\description{
S3 method for \code{\link[=plot]{plot()}} that delegates to \code{\link[=plot_population]{plot_population()}} for objects
of class \code{population}.
}
\examples{
\dontrun{
cohort <- data.frame(
  id = sample(1:1000, 1000, TRUE),
  gender = factor(sample(c("M", "F"), 1000, TRUE)),
  age_band = factor(sample(c("20-29", "30-39", "40-49"), 1000, TRUE),
                    ordered = TRUE)
)
pop <- summarise_population(cohort)
plot(pop)  # equivalent to plot_population(pop)
}

}
\seealso{
\code{\link[=plot_population]{plot_population()}}, \code{\link[=summarise_population]{summarise_population()}}
}
