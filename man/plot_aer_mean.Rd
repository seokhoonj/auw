% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/aer.R
\name{plot_aer_mean}
\alias{plot_aer_mean}
\title{Plot mean A/E Ratio / Cumulative A/E Ratio over elapsed time}
\usage{
plot_aer_mean(
  x,
  value_var = c("caer", "aer"),
  color_type = c("base", "deep"),
  scales = c("fixed", "free_y", "free_x", "free"),
  theme = c("view", "save", "shiny"),
  ...
)
}
\arguments{
\item{x}{An object of class \code{aer_mean} (typically the result of \code{mean(aer_obj)}).}

\item{value_var}{Which metric to plot, one of \code{c("caer", "aer")}. Default shows both in choices; the selected one is drawn.}

\item{color_type}{Color palette selector, one of \code{c("base", "deep")}.
(Reserved for future use; currently not altering scales.)}

\item{scales}{Facet scaling, one of \code{"fixed"}, \code{"free_y"}, \code{"free_x"}, \code{"free"}.}

\item{theme}{One of \code{"view"}, \code{"save"}, \code{"shiny"} passed to \code{ggshort::switch_theme()}.}

\item{...}{Additional arguments forwarded to \code{ggshort::switch_theme()}.}
}
\value{
A \code{ggplot} object.
}
\description{
Visualise group-wise mean AER or CAER with SE bands across the elapsed
dimension. If a \code{gender} column exists, lines are coloured by gender.
}
\details{
Facets are created over \code{attr(x, "group_var")}. The x-axis is the elapsed
variable from \code{attr(x, "elapsed_var")}. SE bands are drawn using the
\verb{*_se_lwr} and \verb{*_se_upp} columns.
}
\examples{
\donttest{
mt <- mean(aer_obj)
plot_aer_mean(mt, value_var = "aer", theme = "view")
}

}
