% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/population.R
\name{plot_population}
\alias{plot_population}
\title{Plot population distribution}
\usage{
plot_population(
  x,
  show_label = TRUE,
  label_args = list(family = getOption("ggshort.font"), size = 4, angle = 90, hjust =
    -0.1, vjust = -0.5, color = "black"),
  theme = c("view", "save", "shiny"),
  ...
)
}
\arguments{
\item{x}{A \code{population} object returned by \code{\link[=summarise_population]{summarise_population()}}.}

\item{show_label}{Logical; if \code{TRUE} (default), display count labels
above each bar.}

\item{label_args}{A named list of arguments controlling label appearance,
passed to \code{\link[ggshort:ggbar]{ggshort::ggbar()}}. Common options include:
\code{family}, \code{size}, \code{angle}, \code{hjust}, \code{vjust}, \code{color}.}

\item{theme}{One of \code{"view"}, \code{"save"}, or \code{"shiny"}. Controls the
ggplot2 theme applied via \code{\link[ggshort:switch_theme]{ggshort::switch_theme()}}.}

\item{...}{Additional arguments forwarded to \code{\link[ggshort:switch_theme]{ggshort::switch_theme()}}.}
}
\value{
A \code{ggplot} object visualising population counts by group.
}
\description{
Creates a bar chart showing the population distribution by age band
and (optionally) gender. Designed for objects created by
\code{\link[=summarise_population]{summarise_population()}}.
}
\details{
\itemize{
\item If a \code{gender} column is present, bars are filled by gender using
\code{\link[ggshort:scale_color_pair_manual]{ggshort::scale_fill_gender()}}.
\item The y-axis is formatted with \code{\link[ggshort:scale_x_comma]{ggshort::scale_y_comma()}} for readability.
\item Label positioning and style can be customized via \code{label_args}.
}
}
\examples{
\dontrun{
cohort <- data.frame(
  id = sample(1:1000, 1000, TRUE),
  gender = factor(sample(c("M", "F"), 1000, TRUE)),
  age_band = factor(sample(c("20-29", "30-39", "40-49"), 1000, TRUE),
                    ordered = TRUE)
)

pop <- summarise_population(cohort)
plot_population(pop)
plot_population(pop, show_label = FALSE, theme = "save")
}

}
\seealso{
\code{\link[=summarise_population]{summarise_population()}}
}
