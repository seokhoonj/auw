% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/relative-risk.R
\name{rr_plot}
\alias{rr_plot}
\title{Relative Risk Plot}
\usage{
rr_plot(
  x,
  logscale = FALSE,
  palette = c("base", "deep"),
  scales = c("fixed", "free_y", "free_x", "free"),
  theme = c("view", "save", "shiny"),
  ...
)
}
\arguments{
\item{x}{An object of class \code{"rr"}.}

\item{logscale}{Logical; if \code{TRUE}, use a log scale on the y-axis.}

\item{palette}{Pair palette key for two-level fills. One of \code{"base"} or \code{"deep"}.
Applied only when \code{decl} has <= 2 levels.}

\item{scales}{Facet scales (used when faceting by \code{gender}):
one of \code{"fixed"} (default), \code{"free"}, \code{"free_x"}, or \code{"free_y"}.}

\item{theme}{Theme key for \code{\link[ggshort:switch_theme]{ggshort::switch_theme()}}:
one of \code{"view"} (default), \code{"save"}, or \code{"shiny"}.}

\item{...}{Additional arguments passed to \code{\link[ggshort:switch_theme]{ggshort::switch_theme()}}.}
}
\value{
A ggplot object.
}
\description{
Visualize relative risks from an \code{rr} object in a compact, consistent style
(mirrors the structure of \code{ir_plot}).
}
\examples{
\dontrun{
p <- rr_plot(rr)  # base plot
p <- rr_plot(rr, logscale = TRUE, palette = "deep")
}
}
