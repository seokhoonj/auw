% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/relative-risk-save.R
\name{save_rr_plans_xlsx}
\alias{save_rr_plans_xlsx}
\title{Save multiple RR plan reports to an Excel workbook}
\usage{
save_rr_plans_xlsx(
  cohort,
  icis,
  plans,
  id_var = id,
  group_var = .(age_band),
  kcd_var = kcd,
  from_var = sdate,
  to_var = edate,
  uw_date = uw_date,
  decl_start_end = c(-60, 0),
  excl_start_end = c(-60, 0),
  claim_start_end = c(0, 36),
  hos_start_end = c(0, 36),
  sur_start_end = c(0, 36),
  add_objects = TRUE,
  file = "RR-plans.xlsx",
  overwrite = FALSE
)
}
\arguments{
\item{cohort}{A data.frame containing the cohort-level input data.}

\item{icis}{A data.frame containing ICIS data used to build mix weights.}

\item{plans}{A data.frame defining one plan per row. Must contain columns
used in the body of the function such as \code{plan}, \code{decl}, \code{excl}, and \code{claim}.}

\item{id_var}{Column in \code{cohort} / \code{icis} identifying individuals.}

\item{group_var}{Grouping column(s) used for summaries. Default \code{.(age_band)}.}

\item{kcd_var}{Column containing diagnosis / code information.}

\item{from_var}{Column containing start dates.}

\item{to_var}{Column containing end dates.}

\item{uw_date}{Underwriting date column or expression resolved by
\code{.resolve_uw_date()}.}

\item{decl_start_end}{Integer length-2 vector giving the declaration window.
Default \code{c(-60, 0)}.}

\item{excl_start_end}{Integer length-2 vector giving the exclusion window.
Default \code{c(-60, 0)}.}

\item{claim_start_end}{Integer length-2 vector giving the claim window.
Default \code{c(0, 36)}.}

\item{hos_start_end}{Integer length-2 vector giving the hospitalisation window.
Default \code{c(0, 36)}.}

\item{sur_start_end}{Integer length-2 vector giving the surgery window.
Default \code{c(0, 36)}.}

\item{add_objects}{Logical; if \code{TRUE}, writes additional reference tables to
each detail sheet. Default \code{TRUE}.}

\item{file}{Output \code{.xlsx} file path. Default \code{"RR-plans.xlsx"}.}

\item{overwrite}{Logical; passed to \code{\link[openxlsx:saveWorkbook]{openxlsx::saveWorkbook()}}.}
}
\value{
Invisibly returns \code{file}.
}
\description{
Builds plan-specific RR report sheets from \code{cohort}, \code{icis}, and \code{plans},
then writes:
}
\details{
\itemize{
\item one detail sheet per plan
\item an index sheet with hyperlinks to all plan sheets
\item summary blocks for relative risk and mean ratios on the index sheet
}

For each row of \code{plans}, this function:
\enumerate{
\item constructs plan-specific declaration / exclusion / claim rules,
\item summarises cohort outcomes and ICIS mix,
\item computes IR / RR / MR summaries,
\item writes a detail sheet via \code{\link[=save_rr_wb]{save_rr_wb()}},
\item collects long-format summary tables used to build index-sheet summaries.
}

After all plans are processed, the function creates:
\itemize{
\item an index sheet with navigation links,
\item wide summary tables for:
\itemize{
\item \code{decision}
\item \code{loading}
\item \code{n}
\item \code{nsum}
\item \code{ratio}
}
}

These summary blocks are written side by side for:
\itemize{
\item Relative Risk
\item Mean Ratio - HOS
\item Mean Ratio - SUR
}

The index sheet is written to sheet \code{"Index"} and contains:
\itemize{
\item an index table with hyperlinks to each plan sheet
\item summary blocks for RR / MR-HOS / MR-SUR
}

Each plan sheet includes a title written at cell \code{B1}, followed by the
standard RR report layout generated by \code{\link[=save_rr_wb]{save_rr_wb()}}.
}
\examples{
\dontrun{
save_rr_plans_xlsx(
  cohort = cohort,
  icis = icis,
  plans = plans,
  file = "RR-plans.xlsx",
  overwrite = TRUE
)
}

}
\seealso{
\code{\link[=save_rr_wb]{save_rr_wb()}}, \code{\link[=save_index_sheet_wb]{save_index_sheet_wb()}}
}
