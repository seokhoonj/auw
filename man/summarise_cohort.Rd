% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/relative-risk.R
\name{summarise_cohort}
\alias{summarise_cohort}
\title{Summarise an episode-level cohort into an ID-level dataset for IR/RR/MR analyses}
\usage{
summarise_cohort(
  cohort,
  id_var,
  group_var,
  kcd_var,
  from_var,
  to_var,
  uw_date,
  decl = NULL,
  decl2 = NULL,
  decl3 = NULL,
  excl = NULL,
  claim = NULL,
  hos = NULL,
  sur = NULL,
  type_var = "type",
  level_var = "level"
)
}
\arguments{
\item{cohort}{A cohort data frame at \strong{episode level} (multiple rows per ID).}

\item{id_var}{ID column; unquoted (e.g., \code{id}) or character (e.g., \code{"id"}).}

\item{group_var}{Optional grouping column (e.g., age band, gender);
unquoted or character. Use \code{NULL} for no grouping.}

\item{kcd_var}{Column with KCD codes; unquoted or character.}

\item{from_var}{Start date column of each episode; unquoted or character.}

\item{to_var}{End date column of each episode; unquoted or character.}

\item{uw_date}{Underwriting date used as time zero; \code{"YYYY-MM-DD"}, \code{Date}, \code{POSIXt},
or a column in \code{cohort} (unquoted or character).}

\item{decl, decl2, decl3}{Optional declaration windows as
\code{list(start_months, end_months, pattern)}.}

\item{excl}{Optional exclusion window, same list form.}

\item{claim}{Optional claim window, same list form.}

\item{hos}{Optional hospitalisation window specification as
\code{list(start_months, end_months, type_value)}. If provided, the function computes
ID-level total hospital days (\code{hos}) and attaches \code{attr(x, "summary.hos")}.}

\item{sur}{Optional surgery window specification as
\code{list(start_months, end_months, type_value)}. If provided, the function computes
ID-level total surgery counts (\code{sur}) and attaches \code{attr(x, "summary.sur")}.}

\item{type_var}{Episode-type column used to identify event categories when \code{hos}
or \code{sur} is provided; unquoted or character. Default is \code{"type"}.}

\item{level_var}{Episode-level column used by surgery logic when \code{sur} is provided;
unquoted or character. Default is \code{"level"}.}
}
\value{
An object of the same base class as \code{cohort}, with prepended class
\code{"id_data"}. One row per ID(+group) with KCD flags and optional outcome columns.
}
\description{
From an episode-level cohort (multiple rows per ID with \verb{[from, to]} and \code{kcd}),
build an \strong{ID-level dataset} with 0/1 KCD flags (decl/excl/claim) around \code{uw_date},
and optionally add ID-level count outcomes such as total hospital days (\code{hos})
and surgery counts (\code{sur}) within specified windows.
}
\details{
Internally converts to data.table (via \code{\link[instead:ensure_dt_env]{instead::ensure_dt_env()}}) for speed, then
restores the main result and attached summaries to the original base class of
\code{cohort} (data.frame / tibble / data.table).

The returned object is an \code{id_data} table (one row per ID(+group)) with:
\itemize{
\item KCD flag columns (e.g. \verb{decl*}, \code{excl}, \code{claim}) from \code{\link[=summarise_id_with_kcd]{summarise_id_with_kcd()}}.
\item Optional count outcomes \code{hos} and/or \code{sur} merged by ID(+group).
}

Attached attributes (restored to the original base class):
\itemize{
\item \code{attr(x, "summary")}: an \code{ir}-class (incidence rate) summary table used by
RR/MR helpers.
\item \code{attr(x, "summary.hos")}, \code{attr(x, "summary.sur")}: \code{mo}-class (mean outcome)
summaries for \code{hos} / \code{sur} when requested.
\item \code{attr(x, "raw")}: lowest-level contingency table.
}

The \code{ir} and \code{mo} summaries carry metadata attributes:
\code{attr(., "decl")}, \code{attr(., "excl")}, \code{attr(., "claim")}.
}
\examples{
\dontrun{
x <- summarise_cohort(
  cohort    = cohort,
  id_var    = id,
  group_var = age_band,
  kcd_var   = kcd,
  from_var  = sdate,
  to_var    = edate,
  uw_date   = "2018-07-01",
  decl      = list(-60, 0,  "I10"),
  decl2     = list(-60, 0,  "E78"),
  excl      = list(-60, 0,  "I2[0-5]|I6[0-9]|G46"),
  claim     = list(  0, 36, "I2[0-5]|I6[0-9]|G46"),
  hos       = list(  0, 36, "hos"),
  sur       = list(  0, 36, "sur")
)
class(x)          # includes "id_data"
class(summary(x)) # includes "ir"
}

}
\seealso{
\code{\link[=summarise_id_with_kcd]{summarise_id_with_kcd()}}, \code{\link[=summarise_id_with_hos]{summarise_id_with_hos()}},
\code{\link[=summarise_id_with_sur]{summarise_id_with_sur()}}
}
