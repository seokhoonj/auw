% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/relative-risk.R
\name{summarise_id_with_sur}
\alias{summarise_id_with_sur}
\title{Summarise IDs with total surgery count within an underwriting window}
\usage{
summarise_id_with_sur(
  cohort,
  id_var,
  group_var,
  from_var,
  to_var,
  type_var,
  level_var,
  uw_date,
  start,
  end,
  type_value = "sur"
)
}
\arguments{
\item{cohort}{A cohort data frame at \strong{episode level} (multiple rows per ID).}

\item{id_var}{ID column; unquoted or character.}

\item{group_var}{Optional grouping column(s); unquoted or character. Use \code{NULL} if none.}

\item{from_var}{Episode start date column; unquoted or character. Must be \code{Date}.}

\item{to_var}{Episode end date column; unquoted or character. Must be \code{Date}.}

\item{type_var}{Column indicating event type (e.g., \code{"hos"}/\code{"sur"}); unquoted or character.}

\item{level_var}{Column indicating surgery level; unquoted or character.
A row is considered surgery only if \code{level_var} is not missing (and not blank for character).}

\item{uw_date}{Underwriting date used as time zero; \code{"YYYY-MM-DD"}, \code{Date}, \code{POSIXt},
or a column in \code{cohort} (unquoted or character).}

\item{start}{Months \strong{before} \code{uw_date} to start the window (negative numeric).}

\item{end}{Months \strong{after} \code{uw_date} to end the window (positive numeric).}

\item{type_value}{Which \code{type_var} value to treat as surgery (default \code{"sur"}).}
}
\value{
An object of the same base class as \code{cohort}, containing one row per
ID(+group) with a single integer column \code{sur} (surgery count in-window).
}
\description{
From an episode-level cohort (multiple rows per ID), compute \strong{ID-level}
total surgery count (\code{sur}) within a window defined in months relative
to \code{uw_date}.
}
\details{
This function:
\enumerate{
\item Subsets rows whose episode interval overlaps the window
\verb{[uw_date + start months, uw_date + end months)} using
\code{to_var >= fdate & from_var < tdate}.
\item Keeps only rows with \code{type_var == type_value} (default \code{"sur"}).
\item Keeps only rows with a non-missing \code{level_var} (and non-blank if character),
because \code{level} indicates a surgery record in your cohort.
\item Counts \strong{one surgery per unique surgery date} per ID(+group) by de-duplicating
on \code{from_var} (typically \code{sdate}).
}
}
\examples{
\dontrun{
sur <- summarise_id_with_sur(
  cohort, id, age_band, sdate, edate, type, level,
  uw_date = "2018-07-01",
  start = 0, end = 36
)
head(sur)
}

}
\seealso{
\code{\link[=summarise_id_with_hos]{summarise_id_with_hos()}}
}
