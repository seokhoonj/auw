% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/relative-risk.R
\name{summarise_mr}
\alias{summarise_mr}
\alias{summarise_mr.id_data}
\title{Summarise mean ratio from \code{id_data} objects}
\usage{
summarise_mr(x, ...)

\method{summarise_mr}{id_data}(
  x,
  outcome = c("hos", "sur"),
  decl_vs = c("0", "1"),
  conf.level = 0.95,
  correction = TRUE,
  ...
)
}
\arguments{
\item{x}{An \code{id_data} object.}

\item{...}{Passed to class-specific methods.}

\item{outcome}{Character scalar. The count outcome column to summarise.
One of \code{"hos"} or \code{"sur"}.}

\item{decl_vs}{Character vector of two declaration levels to compare (default \code{c("0","1")}).}

\item{conf.level}{Confidence level for intervals (default \code{0.95}).}

\item{correction}{Logical. If \code{TRUE}, applies a +0.5 continuity correction
to zero totals for stability.}
}
\value{
A data frame (same base class as the input's restored table) with
prepended class \code{"mr"}. Columns include:
\itemize{
\item \verb{n0, n1}: ID counts in reference vs exposed groups,
\item \verb{v0, v1}: outcome totals in reference vs exposed groups,
\item \verb{rate0, rate1}: per-ID means in each group,
\item \verb{mr, mr_lower, mr_upper}: MR and its Wald confidence interval,
\item \code{p_value}: two-sided Wald test p-value for \code{H0: MR = 1},
\item \code{decision}: factor \code{"reject"} / \code{"fail"} based on \code{conf.level}.
}

Attributes:
\itemize{
\item \code{attr(x, "decl")}, \code{attr(x, "excl")}, and optionally \code{attr(x, "claim")}.
}
}
\description{
Compute mean ratios (MR) with confidence intervals from \code{id_data}
objects by comparing two declaration levels for count outcomes
(e.g., total hospital days \code{hos} or surgeries \code{sur}).
}
\details{
Internally, totals are aggregated per stratum and declaration level,
then MR is computed as \code{(v1/n1) / (v0/n0)} with Wald-type confidence
intervals on the log scale.
}
\examples{
\dontrun{
# x: an 'id_data' object
mh <- summarise_mr(x, outcome = "hos")
ms <- summarise_mr(x, outcome = "sur", decl_vs = c("0","1"))
}

}
